apiVersion: v1
kind: Service
metadata:
  name: service-product
spec:
  type: NodePort
  selector:
    app: service-product
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 32003