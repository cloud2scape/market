apiVersion: v1
kind: Service
metadata:
  name: service-account
spec:
  type: NodePort
  selector:
    app: service-account
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 32001